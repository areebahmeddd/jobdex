# Backend configuration
VITE_API_URL="http://localhost:8000"

# CARTO basemap tile key (get one at carto.com/basemaps/apikey)
# Restricted to production; leave blank for local dev (watermark shown)
VITE_CARTO_KEY=
